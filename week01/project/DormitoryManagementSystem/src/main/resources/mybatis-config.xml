<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 引入 JDBC 外部配置（驱动、URL、用户名、密码）。 -->
    <properties resource="jdbc.properties"/>

    <!-- 设置实体类别名，XML 中可直接用简短类型名。 -->
    <typeAliases>
        <package name="com.gdut.repair.entity"/>
    </typeAliases>

    <!-- 配置数据库运行环境，default 指定默认环境。 -->
    <environments default="development">
        <environment id="development">
        <!-- JDBC 事务管理：由 MyBatis 控制事务边界。 -->
        <transactionManager type="JDBC"/>
        <!-- POOLED 连接池：复用数据库连接，提升性能。 -->
        <dataSource type="POOLED">
            <property name="driver" value="${jdbc.driver}"/>
            <property name="url" value="${jdbc.url}"/>
            <property name="username" value="${jdbc.username}"/>
            <property name="password" value="${jdbc.password}"/>
        </dataSource>
        </environment>
    </environments>

    <!-- 注册 Mapper 映射文件，启动时会加载这些 SQL。 -->
    <mappers>
        <mapper resource="mapper/UserMapper.xml"/>
        <mapper resource="mapper/RepairMapper.xml"/>
    </mappers>
</configuration>

